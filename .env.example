TURSO_DATABASE_URL=libsql://blah
TURSO_AUTH_TOKEN=long blah

# Turso API - For usage metrics in admin panel
# Get org slug: turso org list
# Get API token: turso auth api-tokens mint usage-metrics
TURSO_ORG_SLUG=your-org-slug-here
TURSO_API_TOKEN=your-api-token-here

# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database (for local blahlopment, use SQLite by default)
# DATABASE_URL=sqlite:///db.sqlite3

# Redis (for Channels/WebSockets)
REDIS_URL=redis://127.0.0.1:6379

# Slack integration - REQUIRED for Slack notifications
# Get your token from: https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-token-here

# Base URL for notification links
BASE_URL=http://127.0.0.1:8000
#Old build:  pip install -r requirements.txt && python manage.py collectstatic --noinput &&  python manage.py nuke_turso && python manage.py migrate --run-syncdb --fake-initial && python manage.py load_initial_data && python manage.py create_superuser_if_none
#turso org list #To get the SLUG
#  turso auth api-tokens mint usage-metrics  #to get the API Turso Token                                    